environment:
  matrix:
    - NODE_VERSION: "18"

install:
  - ps: Install-Product node $env:NODE_VERSION x64
  - npm install
  - npm uninstall jest
  - npm install jest@latest

build: off

test_script:
  - npm run test